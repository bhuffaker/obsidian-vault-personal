DOM_Actor 
Function Anim_SexlabWithNPC(Actor akOther, string tag, bool punishment, string reason_name = "")

DOM_API
DOM_Actor Function GetDOMActor(Actor akActor) 

If Game.GetModByName("ZaZAnimationPack.esm") != 255
		ZAZ                    = Game.GetFormFromFile(0x0000CD14, "ZaZAnimationPack.esm") As zbfSexLab
		zbfFactionSlave        = Game.GetFormFromFile(0x000096AE, "ZaZAnimationPack.esm") As Faction

2. You can create a patch from SSEEdit or CK. CK is easiest, just load the mod (do not set it as active file) and edit whatever you want, then save and it will ask you for a new name.
